create table carriers (
carrier_id bigint auto_increment,
name varchar(100),
mc bigint,
dot bigint,
entity_type varchar(100),
primary_first_name varchar(100),
primary_last_name varchar(100),
primary_email varchar(100),
bid_id bigint
);